<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-mybatis.org//DTD Mapper 3//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="steve.practice.mybatis.dao.OrderDao">
    <select id="findOrdersAndUsers" resultMap="orderMap">
        select orders.*,user_info.* from orders,user_info where orders.user_id=user_info.user_id
    </select>
    <resultMap id="orderMap" type="order">
        <id property="id" column="order_id"/>
        <result property="count" column="count"/>
        <result property="status" column="status"/>
        <!--将部分结果与java类映射，需指定java类-->
        <association property="user" javaType="user">
            <id property="id" column="user_id"/>
            <result property="name" column="user_name"/>
            <result property="age" column="age"/>
            <result property="password" column="password"/>
        </association>
    </resultMap>
</mapper>